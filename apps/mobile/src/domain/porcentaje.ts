/**
 * Porcentaje helpers stub (RED phase, US-056 T-04).
 * Will be replaced by the real implementation in T-06.
 */
export const SIN_PORCENTAJE_LABEL = '—';

export function aPorcentajeLabel(_bp: number | null): string {
  throw new Error('Not implemented');
}
