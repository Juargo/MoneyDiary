/**
 * aFechaCorta — stub (RED phase, US-056 T-04).
 * Will be replaced by the real implementation in T-06.
 */
export function aFechaCorta(_fechaIso: string): string {
  throw new Error('Not implemented');
}
