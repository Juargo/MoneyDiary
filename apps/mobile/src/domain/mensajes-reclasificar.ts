/**
 * mensajes-reclasificar stub (RED phase, US-056 T-04).
 * Will be replaced by the real implementation in T-06.
 */
import type { ApiError } from './api-error';

export function mensajeDeErrorReclasificar(_error: ApiError): string {
  throw new Error('Not implemented');
}
