# Seguridad — ver ADR-006
# Cuarentena de 7 días antes de instalar versiones nuevas
minimum-release-age=10080

# Fallar si hay vulnerabilidades HIGH o CRITICAL
audit-level=high

# Bloquear subdependencias de fuentes no verificadas
block-exotic-subdeps=true
