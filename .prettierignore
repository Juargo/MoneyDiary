# apps/api/openapi.json is a DETERMINISTIC build artifact emitted by
# `pnpm api openapi:emit` (schema-sorted keys, fixed formatting). A manual
# `prettier --write` over it would fight the CI drift-check
# (`pnpm api openapi:check`) with false positives — see
# openapi-contract-express design decision on determinism.
apps/api/openapi.json

# packages/api-client/src/types.gen.ts is a DETERMINISTIC build artifact
# generated by `pnpm api-client generate` (openapi-typescript, exact-pinned
# version, fixed flags). Same rationale as apps/api/openapi.json above — a
# Prettier reformat would fight the CI drift gate
# (`git diff --exit-code` on this file) with false positives.
packages/api-client/src/types.gen.ts
