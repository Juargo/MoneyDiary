# Archive Report: api-client-package

**Change**: api-client-package
**Archived**: 2026-08-10
**Status**: COMPLETE (PASS)

## Summary

First slice of ADR-012 (implementation of a platform-agnostic `@moneydiary/api-client` derived from the committed HTTP contract). The change delivers a **types-only, build-less shared workspace package** that eliminates hand-written DTO duplicates in both `apps/web` and `apps/mobile`, with deterministic generation, compile-time money-safety assertions, and a CI drift gate.

**Delivery**: 3 chained PRs (stacked-to-main), all merged to `main` with green CI including the new `api-client` drift-gate job and the `packages`-triggered web/mobile fan-out. No CRITICAL or WARNING findings.

## Artifacts

### Change artifacts (closed)
- `openspec/changes/api-client-package/proposal.md` — intent, scope, approach, risks, slicing
- `openspec/changes/api-client-package/design.md` — architecture, package anatomy, codegen pipeline, adoption mapping, mobile specifics, ADR-012 amendment
- `openspec/changes/api-client-package/tasks.md` — 12 phases, 61 implementation tasks, all complete
- `openspec/changes/api-client-package/verify-report.md` — verification: PASS, 21/21 spec scenarios compliant, 0 CRITICAL/WARNING

### Delta specs → main specs (synced)
| Domain | Action | Requirements |
|--------|--------|--------------|
| `api-client` (new) | Created | AC-01 (identity + workspace + no-runtime) · AC-02 (generated from contract) · AC-03 (deterministic, committed, CI drift) · AC-04 (money fields string-typed, compile-time assertion) · AC-05 (build-less surface) · AC-06 (packages-only changes trigger CI) |
| `web-app` | Merged | WAC-01 (web DTOs derived, not hand-written) · WAC-02 (web guards/errors unchanged) |
| `mobile-app` (new) | Created | MAC-01 (mobile DTOs derived, not hand-written) · MAC-02 (verbatimModuleSyntax erasure guarantee) · MAC-03 (mobile guards/errors/conTimeout unchanged) |

**Merged into main specs**:
- `openspec/specs/api-client/spec.md` — created from delta, no prior main spec
- `openspec/specs/web-app/spec.md` — appended WAC-01/WAC-02 to existing WCAT/WPER/WMYP/DCR requirements
- `openspec/specs/mobile-app/spec.md` — created from delta, no prior main spec (ratified location: cross-cutting, independent of mobile-resumen-screen / mobile-session-auth)

## What Was Delivered

### Package (`packages/api-client` — PR #264, Slice 1)
- New pnpm workspace member `@moneydiary/api-client`, listed in `pnpm-workspace.yaml`
- `package.json` with `types`-only `exports` (no `default`/`import`/`main` entry — runtime import fails deliberately)
- `tsconfig.json` standalone (strict, noEmit, no ambient types)
- `src/index.ts` — hand-curated alias layer (15 one-line indexed-access type aliases + re-export of `paths`/`components`/`operations`)
- `src/types.gen.ts` — committed (not gitignored), generated by `openapi-typescript@7.13.0 --immutable`
- `src/money-contract.assert.ts` — compile-time assertions (10 scenarios pinning all decimal-string fields to `string`)
- CI wiring: new `packages` path filter + new `api-client` job (regenerate → `git diff --exit-code` drift gate) + web/mobile fan-out on packages-only changes
- ADR-012 amendment note (dated 2026-08-09, Spanish, appended to the ADR)
- Root convenience scripts: `api-client` + `contract:sync` pnpm shortcuts

**All AC-01..06 scenarios verified PASS**. No hand-written DTO interfaces remain in web; no hand-written mobile DTO declarations remain; money assertions compile; drift gate proves determinism; consumers typecheck without a package build step; packages-only changes trigger web+mobile CI.

### Web adoption (PR #265, Slice 2)
- `apps/web/src/api/types.ts` rewritten as alias barrel (238 → 171 lines; 13 direct aliases, 1 narrowed alias, 1 hand-written UI projection)
- `@moneydiary/api-client: workspace:*` declared in `package.json`
- Fixtures updated (`capabilities.test.tsx`, enum-narrowing fallout in `client.test.ts`)
- Guard tests pass unmodified (WAC-02 "confirm existing tests are the RED-equivalent baseline" baseline captured then matched post-migration)

**All WAC-01/02 scenarios verified PASS**. Web typecheck green (560 tests passing). No fetch logic, `ApiError` unions, or runtime guards edited.

### Mobile adoption (PR #266, Slice 3)
- Metro/jest-expo spike executed (3 steps: TS resolution ✅, jest-expo transform ✅, `expo export --platform android` GO ✅) before any mobile code change
- `apps/mobile/tsconfig.json`: `verbatimModuleSyntax: true` added
- `@moneydiary/api-client: workspace:*` declared in `package.json`
- `apps/mobile/src/domain/resumen.types.ts`, `src/api/client.ts`, `src/api/post-ingesta.ts`, `src/api/preview-ingesta.ts` aliased (5 hand-written mobile files)
- Fixtures updated (`client.spec.ts`, `session-context.spec.tsx`, `auth-navigation.integration.spec.tsx`, `post-ingesta.spec.ts`, `app/subir.spec.tsx`)
- Guard tests pass unmodified (MAC-03 "confirm existing tests are the RED-equivalent baseline" baseline: 27 suites, 236 tests, post-migration: 27 suites, 236 tests)

**All MAC-01..03 scenarios verified PASS**. Mobile typecheck green. No fetch/session logic, `ApiError`, or `conTimeout` edited. Type imports are all `import type {...}` form, erased pre-Metro.

## Verification Summary

**Completeness**: 61/61 tasks complete (all phases, all slices green)
**Build & Tests**: All pass live (not trusted from report alone; re-run verified)
- `packages/api-client typecheck`: PASS
- Web `typecheck`: PASS · `test`: PASS (560/560)
- Mobile `typecheck`: PASS · `test`: PASS (236/236, byte-identical baseline)
- Drift gate: PASS (regenerate → byte-identical MD5 `6ae0ea4fb7ba61b08c70fb7b9c30b61f`)

**Spec Compliance**: 11 requirements, 21 scenarios, all COMPLIANT
- AC-01..06 (api-client package)
- WAC-01/02 (web DTOs + guards)
- MAC-01..03 (mobile DTOs + erasure + guards)

**TDD**: Strict TDD Mode active; package itself has no new tests by design (types-only); web/mobile suites pre-migration baseline captured, post-migration verified identical count + all passing; guard tests unedited, passing per spec scenario requirement

**Issues**: 0 CRITICAL, 0 WARNING, 1 SUGGESTION (login.spec.tsx flake not reproduced across 7 runs; not currently blocking; pre-existing noise in logs unrelated to this change)

## Delivery

| PR | Slice | Link | Status | Changes |
|----|-------|------|--------|---------|
| #264 | 1. Package + CI foundation | stacked → main | ✅ Merged | ~228 hand-written + ~700-1000 generated (build artifact) |
| #265 | 2. Web adoption | stacked → main | ✅ Merged | ~340 hand-written (238 del / 100 add) |
| #266 | 3. Mobile adoption | stacked → main | ✅ Merged | ~140 hand-written |

**Chain strategy**: stacked-to-main (each PR → `main`, ADR-031). Dependency order: 1 → 2, 1 → 3 (2⊥3 independent). All merged by 2026-08-10.

**Judgment Day** (adversarial review): all 3 PRs APPROVED (motion: "all work complete and correctly implemented per spec").

## Deferred Debt (Registered in Tasks)

| Debt | Trigger |
|------|---------|
| Runtime client (`client.ts`, interceptors, openapi-fetch) | Next ADR-012 slice; mobile as first adopter |
| `TokenStorage` port + DI wiring | Next ADR-012 slice; mobile first |
| Unified `errors.ts` taxonomy | Requires product sign-off on user-facing error copy reconciliation |
| Mobile `esMeDto` rejects demo accounts with `email: null` as `{tag:'parse'}` | Pre-existing runtime behavior, unchanged by this slice; already known, no new trigger |

## Risks — None Realized

| Risk | Mitigation | Outcome |
|------|-----------|---------|
| Metro cannot resolve pnpm-symlinked workspace package | 3-step spike (TS, jest, Metro bundle), `verbatimModuleSyntax`, bounded remedy | ✅ GO — types erased pre-bundle as expected |
| `openapi-typescript` inside 7-day quarantine | Pin to newest 7.x with ≥8-day publish date (7.13.0, 2026-07-31 publish) | ✅ Installed cleanly |
| `--immutable` flag absent/renamed | Verify with `--help` in pinned version | ✅ Present in 7.13.0 |
| Non-deterministic emit → false drift failures | Exact pin + deterministic input + fixed flags + `.prettierignore` + no lint route | ✅ Regenerated byte-identical |
| Enum narrowing (`estadoSemaforo`) breaks fixtures | Mechanical fixes at call sites, not alias layer | ✅ Fixtures updated, gates working |
| `verbatimModuleSyntax` cascades pre-existing type-import violations | Expected small; contingency = ESLint enforcement + debt | ✅ Zero cascade, flag adopted cleanly |

## Notes

**No ADR changes.** ADR-012 decision (platform-agnostic shared client, DI, no npm publication) stands. The 2026-08-09 amendment note clarifies mechanics only (types-only first slice, no `tsup`, committed `types.gen.ts`), not the decision itself.

**Message to future slices**: When the runtime client (`client.ts` + `TokenStorage` + `errors.ts` unification) lands, it will build on this foundation. `tsup` and `.gitignore` rules for `types.gen.ts` will be re-evaluated then (both currently deliberate out-of-scope decisions tied to the types-only nature of this slice). Mobile is the natural first adopter of the token-storage DI pattern.

## Archive Location

- **`openspec/changes/archive/2026-08-10-api-client-package/`** — full change folder (proposal, design, tasks, verify report, delta specs) for historical audit trail
- **`openspec/specs/{api-client,web-app,mobile-app}/spec.md`** — main specs (source of truth going forward)

---

**Archived by**: sdd-archive executor
**Date**: 2026-08-10
**Verdict**: ✅ PASS — All 11 spec requirements verified PASS across 21 scenarios. All 61 implementation tasks complete. 3 PRs merged with green CI. Ready for next change. SDD cycle closed for `api-client-package`.
